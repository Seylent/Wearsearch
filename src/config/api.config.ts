/**
 * API Configuration
 * Central place for API URLs to support network access from other devices
 */

// Use relative URL so it works through ngrok and proxies
export const API_BASE_URL = '';
export const API_URL = '/api';
