# 🔧 Виправлення помилки авторизації (401 Unauthorized)

## ❌ Проблема
```
GET http://localhost:5173/api/v1/auth/me 401 (Unauthorized)
```

Запити йдуть на `localhost:5173` (Vite dev server) замість backend API.

## ✅ Рішення

### Крок 1: Перевірте backend
```bash
# Backend має бути запущений!
# За замовчуванням: http://localhost:3001
```

Переконайтеся, що backend працює та відповідає на:
- `GET http://localhost:3001/api/v1/auth/me`

### Крок 2: Оновлено файл `.env.local`

Я вже оновив файл `.env.local` з правильними налаштуваннями:

```env
NEXT_PUBLIC_API_URL=http://localhost:3001
API_PROXY_TARGET=http://localhost:3001
NEXT_PUBLIC_API_BASE_URL=/api/v1
PORT=3000
```

### Крок 3: ОБОВ'ЯЗКОВО перезапустіть dev сервер!

```bash
# Зупиніть поточний dev сервер (Ctrl+C)

# Очистіть кеш Next.js
Remove-Item -Recurse -Force .next

# Запустіть знову
npm run dev
```

**ВАЖЛИВО**: Next.js читає `.env` файли ТІЛЬКИ при старті. Якщо ви змінили `.env.local`, ОБОВ'ЯЗКОВО перезапустіть сервер!

### Крок 4: Перевірте що сервер працює на правильному порту

Після запуску має бути:
```
✓ Ready on http://localhost:3000
```

НЕ `localhost:5173` (це Vite, не Next.js)!

### Крок 5: Очистіть кеш браузера

```
1. Відкрийте DevTools (F12)
2. Правий клік на кнопці оновлення
3. Виберіть "Empty Cache and Hard Reload"
```

Або просто використайте incognito режим для тестування.

## 🔍 Перевірка

### 1. Перевірте що backend працює:
```bash
# У новому терміналі
curl http://localhost:3001/api/v1/statistics
```

Має повернути JSON з статистикою.

### 2. Перевірте проксі Next.js:
```bash
# Коли frontend запущений на localhost:3000
curl http://localhost:3000/api/v1/statistics
```

Має повернути ті ж дані (проксі працює).

### 3. Спробуйте увійти:

1. Відкрийте http://localhost:3000/auth/login
2. Введіть дані для входу
3. Перевірте консоль браузера - не має бути помилок 401

## 🐛 Якщо все ще не працює

### Проблема 1: Backend не запущений
```bash
# Перейдіть до backend папки
cd c:\backend\Wearsearch-api-main

# Запустіть backend
npm run dev
```

### Проблема 2: Порт 3001 зайнятий
```bash
# Знайдіть процес що використовує порт
netstat -ano | findstr :3001

# Закрийте процес (замініть PID)
taskkill /F /PID <PID_NUMBER>
```

### Проблема 3: CORS помилки
Якщо бачите CORS помилки, переконайтеся що backend дозволяє `http://localhost:3000`:

```javascript
// У backend: server.js або app.js
app.use(cors({
  origin: ['http://localhost:3000', 'http://localhost:5173'],
  credentials: true
}));
```

### Проблема 4: Токен авторизації відсутній
```javascript
// У консолі браузера
localStorage.getItem('wearsearch.auth')
```

Якщо `null` - потрібно залогінитись знову.

## 📝 Що було змінено

### До:
```env
PORT=5173                                    ❌ Vite порт
API_PROXY_TARGET=http://localhost:3000      ❌ Неправильний порт
NEXT_PUBLIC_API_URL=http://localhost:3000   ❌ Неправильний порт
```

### Після:
```env
PORT=3000                                    ✅ Next.js порт
API_PROXY_TARGET=http://localhost:3001      ✅ Правильний backend
NEXT_PUBLIC_API_URL=http://localhost:3001   ✅ Правильний backend
NEXT_PUBLIC_API_BASE_URL=/api/v1            ✅ Відносний URL (проксі)
```

## 🎯 Результат

Після виправлення:
- ✅ Запити йдуть на `/api/v1/auth/me` (відносний URL)
- ✅ Next.js проксі перенаправляє на `http://localhost:3001/api/v1/auth/me`
- ✅ Backend обробляє запит
- ✅ Авторизація працює!

## ⚡ Швидке виправлення (TL;DR)

```bash
# 1. Перезапустіть frontend
Ctrl+C
Remove-Item -Recurse -Force .next
npm run dev

# 2. Переконайтеся що frontend на порту 3000
# 3. Переконайтеся що backend на порту 3001
# 4. Очистіть кеш браузера (Ctrl+Shift+R)
# 5. Спробуйте залогінитись знову
```

---

**Дата**: 15 січня 2026  
**Проблема**: 401 Unauthorized на `/api/v1/auth/me`  
**Причина**: Неправильна конфігурація портів  
**Рішення**: Оновлено `.env.local` + перезапуск сервера
