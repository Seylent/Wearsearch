# 🔍 Пошук магазинів у navbar - Вимоги до Backend

**Дата:** 3 січня 2026  
**Функція:** Розширення пошуку для включення магазинів  
**Пріоритет:** Середній  

---

## 📋 Загальний опис

Додано можливість шукати магазини у navbar поруч з продуктами. Користувач може ввести назву магазину в пошук, натиснути на результат і перейти на сторінку всіх продуктів від цього магазину.

---

## ✅ Що вже зроблено на Frontend

### 1. Оновлено пошуковий хук (`useProductSearch.ts`)
- ✅ Додано паралельний запит для пошуку магазинів
- ✅ Використовується існуючий endpoint `/stores` (GET)
- ✅ Фільтрація магазинів по назві на frontend
- ✅ Обмеження до 3 магазинів у результатах
- ✅ Об'єднання результатів: спочатку магазини, потім продукти

### 2. Оновлено UI компоненти
- ✅ Магазини мають інший стиль (світліший фон, іконка магазину)
- ✅ Показується бейдж "STORE" для магазинів
- ✅ При кліку на магазин → перехід на `/products?store_id={id}`
- ✅ Показується кількість продуктів магазину (якщо доступно)

### 3. Логіка переходу
```tsx
// При кліку на магазин
navigate(`/products?store_id=${storeId}`);

// Це вже працює - Products сторінка вміє фільтрувати по store_id
```

---

## 🚀 Що потрібно зробити на Backend (Опціонально)

### Варіант 1: Використовувати існуючу логіку (Рекомендовано)

**Поточний стан:** Frontend вже працює з існуючим endpoint `/stores`

✅ **Що вже є:**
- Endpoint: `GET /stores`
- Повертає всі магазини
- Frontend фільтрує по назві локально

✅ **Переваги:**
- Не потрібно нічого міняти на backend
- Все вже працює
- Проста реалізація

❌ **Недоліки:**
- Завантажується список всіх магазинів (може бути багато)
- Фільтрація на frontend (не оптимально для великої кількості)

---

### Варіант 2: Додати search параметр (Оптимізація)

**Рекомендовано якщо магазинів буде більше 100+**

#### Додати підтримку пошуку до існуючого endpoint

**Endpoint:** `GET /stores`

**Новий query параметр:**
```
GET /stores?search=<query>
```

**Приклад запиту:**
```bash
# Шукати магазини з "zara" в назві
GET /stores?search=zara
```

**Відповідь (без змін):**
```json
[
  {
    "id": "1",
    "name": "Zara Official",
    "telegram_url": "https://t.me/zara",
    "instagram_url": "https://instagram.com/zara",
    "logo_url": "https://...",
    "is_recommended": true,
    "product_count": 150
  }
]
```

**SQL запит (приклад):**
```sql
-- PostgreSQL
SELECT * FROM stores 
WHERE LOWER(name) LIKE LOWER($1)
ORDER BY is_recommended DESC, name ASC
LIMIT 10;

-- Параметр: %zara%
```

**Логіка на Backend:**
```python
# Python/FastAPI приклад
@app.get("/stores")
async def get_stores(
    search: Optional[str] = None,
    db: Session = Depends(get_db)
):
    query = db.query(Store)
    
    # Якщо є search параметр - фільтруємо
    if search:
        search_term = f"%{search.lower()}%"
        query = query.filter(
            func.lower(Store.name).like(search_term)
        )
    
    # Сортування: recommended спочатку
    query = query.order_by(
        Store.is_recommended.desc(),
        Store.name.asc()
    )
    
    stores = query.limit(10).all()
    return stores
```

---

### Варіант 3: Створити окремий endpoint (Найкраще для складного пошуку)

**Новий endpoint:** `GET /stores/search`

**Query параметри:**
```
GET /stores/search?q=<query>&limit=5
```

**Приклади:**
```bash
# Простий пошук
GET /stores/search?q=zara&limit=5

# З мінімальною довжиною запиту
GET /stores/search?q=za&limit=3
```

**Відповідь:**
```json
{
  "success": true,
  "data": [
    {
      "id": "1",
      "name": "Zara Official",
      "logo_url": "https://...",
      "product_count": 150,
      "is_recommended": true,
      "match_score": 0.95  // Опціонально - для релевантності
    }
  ],
  "total": 1,
  "query": "zara"
}
```

**Переваги:**
- Можна додати складнішу логіку (fuzzy search, relevance score)
- Можна шукати не тільки по назві, але й по опису, тегах
- Окремий endpoint не впливає на існуючий `/stores`

**SQL з релевантністю (PostgreSQL):**
```sql
-- З використанням Full-Text Search
SELECT 
  s.*,
  ts_rank(
    to_tsvector('simple', s.name),
    plainto_tsquery('simple', $1)
  ) as match_score
FROM stores s
WHERE 
  to_tsvector('simple', s.name) @@ plainto_tsquery('simple', $1)
  OR LOWER(s.name) LIKE LOWER($2)
ORDER BY match_score DESC, is_recommended DESC
LIMIT $3;

-- Параметри: "zara", "%zara%", 5
```

---

## 🔧 Зміни на Backend (Рекомендований план)

### Крок 1: Оновити існуючий endpoint `/stores`

**Файл (приклад):** `routes/stores.py` або `controllers/store_controller.py`

**Було:**
```python
@app.get("/stores")
async def get_stores(db: Session = Depends(get_db)):
    stores = db.query(Store).all()
    return stores
```

**Стало:**
```python
@app.get("/stores")
async def get_stores(
    search: Optional[str] = Query(None, min_length=2, max_length=100),
    limit: int = Query(10, ge=1, le=100),
    db: Session = Depends(get_db)
):
    query = db.query(Store)
    
    # Пошук якщо є search параметр
    if search:
        search_pattern = f"%{search.lower()}%"
        query = query.filter(
            func.lower(Store.name).like(search_pattern)
        )
    
    # Сортування
    query = query.order_by(
        Store.is_recommended.desc(),
        Store.name.asc()
    )
    
    stores = query.limit(limit).all()
    
    # Додати product_count якщо ще немає
    for store in stores:
        if not hasattr(store, 'product_count'):
            store.product_count = db.query(ProductStore)\
                .filter(ProductStore.store_id == store.id)\
                .count()
    
    return stores
```

### Крок 2: Переконатися що product_count повертається

**Модель Store повинна включати:**
```python
class Store(Base):
    __tablename__ = "stores"
    
    id = Column(String, primary_key=True)
    name = Column(String, nullable=False)
    telegram_url = Column(String)
    instagram_url = Column(String)
    logo_url = Column(String)
    is_recommended = Column(Boolean, default=False)
    created_at = Column(DateTime)
    updated_at = Column(DateTime)
    
    # Relationship для підрахунку продуктів
    products = relationship("ProductStore", back_populates="store")
    
    @property
    def product_count(self):
        """Кількість продуктів у магазині"""
        return len(self.products)
```

**Або через SQL join:**
```python
from sqlalchemy import func

stores_with_counts = db.query(
    Store,
    func.count(ProductStore.product_id).label('product_count')
).outerjoin(
    ProductStore, Store.id == ProductStore.store_id
).group_by(
    Store.id
).all()
```

### Крок 3: Тестування

**Тест 1: Пошук працює**
```bash
curl "http://localhost:8000/stores?search=zara"
```

**Тест 2: Без пошуку повертає все**
```bash
curl "http://localhost:8000/stores"
```

**Тест 3: Limit працює**
```bash
curl "http://localhost:8000/stores?search=shop&limit=3"
```

**Очікуваний результат:**
```json
[
  {
    "id": "1",
    "name": "Zara Official Store",
    "telegram_url": "https://t.me/zara",
    "instagram_url": "https://instagram.com/zara",
    "logo_url": "https://cdn.example.com/zara-logo.png",
    "is_recommended": true,
    "product_count": 150,
    "created_at": "2024-01-01T00:00:00Z",
    "updated_at": "2024-01-01T00:00:00Z"
  }
]
```

---

## 🎯 Пріоритети

### Мінімально (Працює зараз)
- ✅ Нічого не робити - frontend фільтрує локально
- ✅ Все працює з існуючим `/stores`

### Рекомендовано (Оптимізація)
- 🔄 Додати `search` параметр до `/stores`
- 🔄 Переконатися що `product_count` повертається
- 🔄 Тестування

### Опціонально (Майбутнє)
- 📅 Додати fuzzy search (typo tolerance)
- 📅 Додати пошук по опису магазину
- 📅 Додати пошук по тегах/категоріях магазину
- 📅 Додати аналітику пошукових запитів

---

## 📊 Очікувана поведінка користувача

1. **Користувач відкриває пошук** (клік на іконку Search у navbar)
2. **Вводить назву магазину** (наприклад "zara")
3. **Бачить результати:**
   - 1-3 магазини (з бейджем "STORE")
   - 5+ продуктів
4. **Клікає на магазин**
5. **Переходить на** `/products?store_id=1`
6. **Бачить всі продукти** від цього магазину

---

## 🔍 Frontend реалізація (для розуміння)

```typescript
// Hook запит магазинів
const { data: storesData } = useQuery({
  queryKey: ['stores-search', searchQuery],
  queryFn: async () => {
    const allStores = await storeService.getAllStores();
    const query = searchQuery.toLowerCase().trim();
    
    // Фільтрація на frontend (поки backend не додасть search)
    return allStores.filter(store => 
      store.name.toLowerCase().includes(query)
    ).slice(0, 3);
  },
  enabled: searchQuery.length >= 2,
});

// При кліку на магазин
const handleStoreClick = (storeId: string) => {
  navigate(`/products?store_id=${storeId}`);
};
```

---

## 🧪 Приклади запитів

### Frontend робить такі запити:

**1. Отримання всіх магазинів (поточний):**
```http
GET /stores
```

**2. Після додання search (майбутнє):**
```http
GET /stores?search=zara
```

### Products page вже підтримує фільтрацію:
```http
GET /items?store_id=1&page=1&limit=24
```

---

## ⚠️ Важливо

### 1. Існуючі endpoint не зламаються
- `GET /stores` без параметрів працює як зараз
- Додавання `search` параметра - опціонально
- Backward compatible

### 2. Products сторінка вже працює
- Фільтрація по `store_id` вже реалізована
- Тестували при розробці функції магазинів
- Нічого не потрібно міняти

### 3. Performance
- Зараз: завантажується весь список магазинів (~10-50 записів)
- Після оптимізації: тільки релевантні результати
- Якщо магазинів < 100 - не критично

---

## 🎨 UI приклад результатів пошуку

```
┌─────────────────────────────────────────────┐
│  🔍 Search                              ✕   │
├─────────────────────────────────────────────┤
│                                             │
│  ╔═══════════════════════════════════════╗ │
│  ║ [LOGO] 🏪 STORE                       ║ │
│  ║        Zara Official Store            ║ │
│  ║        150 products → View all        ║ │
│  ╚═══════════════════════════════════════╝ │
│                                             │
│  ┌───────────────────────────────────────┐ │
│  │ [IMG]  Zara White T-Shirt        ₴500│ │
│  │        Tops • Zara                    │ │
│  └───────────────────────────────────────┘ │
│                                             │
│  ┌───────────────────────────────────────┐ │
│  │ [IMG]  Zara Black Jeans          ₴800│ │
│  │        Bottoms • Zara                 │ │
│  └───────────────────────────────────────┘ │
│                                             │
└─────────────────────────────────────────────┘
```

**Магазини:** Світліший фон, іконка 🏪, бейдж "STORE"  
**Продукти:** Звичайний вигляд, ціна справа

---

## 📝 Checklist для Backend розробника

### Базова реалізація (Рекомендовано)
- [ ] Додати `search: Optional[str]` параметр до `/stores`
- [ ] Додати фільтрацію по назві магазину (LIKE query)
- [ ] Додати `limit` параметр (за замовчуванням 10)
- [ ] Переконатися що `product_count` повертається
- [ ] Тестування: `/stores?search=zara`
- [ ] Тестування: `/stores` (без параметрів)

### Опціонально
- [ ] Додати сортування по релевантності
- [ ] Додати fuzzy search (подібність)
- [ ] Додати аналітику пошукових запитів
- [ ] Додати кешування результатів пошуку

---

## 🤝 Комунікація

**Питання?** Пиши у чат команди або створи issue

**Frontend готовий:** Працює з поточним API, оптимізація опціональна

**Часова оцінка:**
- Базова реалізація: 30-60 хвилин
- З тестами: 1-2 години
- З повною оптимізацією: 3-4 години

---

**Створено:** 3 січня 2026  
**Frontend version:** 1.0 (Готово ✅)  
**Backend action required:** Опціонально (Оптимізація)
