# 🔍 Діагностика пошуку магазинів

## Проблема
Магазин "wlockerstore" не показується в результатах пошуку

## ✅ Що зроблено для виправлення

### 1. Покращено логіку пошуку
- ✅ Додано нормалізацію назв (видаляє пробіли, спецсимволи)
- ✅ Пошук працює як з оригінальною назвою, так і з нормалізованою
- ✅ Не чутливий до регістру

### 2. Додано детальне логування
Тепер в консолі браузера (F12) ти побачиш:
```
[StoreService] Fetching all stores...
[StoreService] Response: [...]
[StoreService] Returned array of stores: 10
[Search] All stores: 10 ['Store 1', 'Store 2', ...]
[Search] Query: wlocker
[Search] Filtered stores: 1 ['wlockerstore']
```

---

## 🔧 Як діагностувати проблему

### Крок 1: Відкрий консоль браузера
1. Натисни `F12` або `Ctrl+Shift+I`
2. Перейди на вкладку **Console**

### Крок 2: Відкрий пошук
1. Клікни на іконку 🔍 Search у navbar
2. Почни вводити "wlocker"

### Крок 3: Дивись логи в консолі

**Якщо бачиш:**
```
[StoreService] Fetching all stores...
[StoreService] Response: []
```
➡️ **Проблема:** Backend не повертає магазини
➡️ **Рішення:** Перевір backend endpoint `/stores`

**Якщо бачиш:**
```
[Search] All stores: 0
```
➡️ **Проблема:** Магазини не завантажуються з API
➡️ **Рішення:** Перевір:
- Чи запущений backend?
- Чи є магазини в базі даних?
- Чи правильний URL до API?

**Якщо бачиш:**
```
[Search] All stores: 10 ['Store A', 'Store B', ...]
[Search] Filtered stores: 0
```
➡️ **Проблема:** Магазин є, але не знаходиться за запитом
➡️ **Рішення:** Перевір як називається магазин в базі даних

**Якщо бачиш:**
```
[Search] All stores: 10 ['wlockerstore', ...]
[Search] Filtered stores: 1 ['wlockerstore']
```
✅ **Все працює!** Магазин має показатися в результатах

---

## 🐛 Можливі проблеми та рішення

### Проблема 1: Backend не повертає магазини
**Симптом:**
```
[StoreService] Error fetching stores: Network Error
```

**Рішення:**
1. Перевір чи запущений backend
2. Перевір URL в `src/config/api.config.ts`
3. Перевір CORS налаштування на backend

**Тест:**
```bash
# Перевір чи працює API
curl http://localhost:8000/stores
```

---

### Проблема 2: Магазин називається інакше
**Симптом:**
```
[Search] All stores: ['Wlocker Store', 'Another Store']
[Search] Query: wlockerstore
[Search] Filtered stores: 0
```

**Причина:** Магазин в базі називається "Wlocker Store" (з пробілом), а ти шукаєш "wlockerstore"

**Рішення:** Покращена логіка пошуку тепер це враховує!
- `wlockerstore` → знайде `Wlocker Store`
- `wlocker` → знайде `Wlocker Store` і `wlockerstore`

---

### Проблема 3: Магазин є, але product_count = 0
**Симптом:** Магазин показується, але написано "0 products"

**Причина:** Backend не повертає `product_count` або в магазині немає продуктів

**Рішення:**
1. Додай продукти до магазину через admin панель
2. Попроси backend розробника повернути `product_count` в `/stores`

---

### Проблема 4: Результатів немає взагалі
**Симптом:** Вводиш текст, але нічого не показується

**Можливі причини:**
1. **Запит занадто короткий** - потрібно мінімум 2 символи
2. **Debounce затримка** - почекай 300мс після введення
3. **React Query кеш** - спробуй оновити сторінку

**Рішення:**
```javascript
// Очисти кеш в консолі браузера
localStorage.clear();
location.reload();
```

---

## 🧪 Тестування

### Тест 1: Простий пошук
```
Введи: "wlocker"
Очікується: Магазин "wlockerstore" показується
```

### Тест 2: Пошук з пробілами
```
Введи: "wlocker store"
Очікується: Магазин "wlockerstore" або "Wlocker Store" показується
```

### Тест 3: Пошук з великими літерами
```
Введи: "WLOCKER"
Очікується: Магазин знаходиться (не чутливо до регістру)
```

### Тест 4: Частковий пошук
```
Введи: "wlo"
Очікується: Магазини що містять "wlo" в назві
```

---

## 📊 Debug команди для консолі браузера

### Подивитись всі магазини
```javascript
// В консолі браузера
fetch('http://localhost:8000/stores')
  .then(r => r.json())
  .then(data => console.table(data));
```

### Перевірити React Query кеш
```javascript
// В консолі браузера після відкриття пошуку
// Покаже всі закешовані запити
window.__REACT_QUERY_DEVTOOLS_GLOBAL_HOOK__
```

### Очистити кеш магазинів
```javascript
// В консолі браузера
localStorage.removeItem('stores-cache');
location.reload();
```

---

## 🔍 Що робити далі

### 1. Спробуй зараз
1. Збережи файли (`Ctrl+S`)
2. Оновіть сторінку (`F5`)
3. Відкрий пошук
4. Введи "wlocker"
5. Дивись консоль (`F12`)

### 2. Якщо не працює
Скопіюй логи з консолі та покажи мені:
```
[StoreService] ...
[Search] All stores: ...
[Search] Query: ...
[Search] Filtered stores: ...
```

### 3. Перевір backend
```bash
# Запит до backend
curl http://localhost:8000/stores

# Має повернути список магазинів з "wlockerstore"
```

---

## ✅ Checklist

- [ ] Сторінка оновлена (`F5`)
- [ ] Консоль браузера відкрита (`F12`)
- [ ] Backend запущений
- [ ] Введено мінімум 2 символи в пошук
- [ ] Почекано 300мс після введення
- [ ] Перевірено логи в консолі
- [ ] Магазин "wlockerstore" є в базі даних

---

## 📝 Для backend розробника

Якщо магазин не знаходиться, перевір:

```sql
-- PostgreSQL: Перевір чи є магазин в базі
SELECT id, name, is_recommended, product_count 
FROM stores 
WHERE LOWER(name) LIKE '%wlocker%';

-- Має повернути:
-- id | name         | is_recommended | product_count
-- 1  | wlockerstore | true          | 42
```

Переконайся що endpoint повертає магазини:
```bash
curl http://localhost:8000/stores
```

---

**Зроблено:** 3 січня 2026  
**Статус:** ✅ Покращено логіку пошуку + додано логування  
**Наступний крок:** Перевір консоль браузера
