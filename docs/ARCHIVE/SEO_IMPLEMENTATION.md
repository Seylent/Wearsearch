# SEO-Оптимізація: Впроваджено

## ✅ Виконані завдання згідно SEO-ТЗ

### 1. Створено SEO-утиліти
- **`src/lib/seo/metadata-utils.ts`** — функції для генерації title, description та metadata
  - `generateSEOTitle()` — формула: "Основний запит + уточнення | Wearsearch"
  - `generateSEODescription()` — формула: "Що це + користь + дія"
  - `generateCategoryMetadata()` — повні метадані для категорій
  - `generateBrandMetadata()` — повні метадані для брендів
  - `generateProductMetadata()` — повні метадані для продуктів
  - `generateSearchMetadata()` — метадані з noindex для пошуку
  - `generateHomeMetadata()` — метадані для головної сторінки

- **`src/lib/seo/structured-data.ts`** — JSON-LD structured data
  - `generateOrganizationSchema()` — схема організації
  - `generateWebSiteSchema()` — схема сайту з пошуком
  - `generateProductSchema()` — схема продукту
  - `generateBreadcrumbSchema()` — хлібні крихти
  - `<JsonLd />` — компонент для вставки на сторінку

### 2. Оновлено metadata основних сторінок
- **`src/app/page.tsx`** — головна сторінка використовує `generateHomeMetadata()`
- **`src/app/products/[id]/page.tsx`** — сторінки продуктів використовують `generateProductMetadata()`
- **`src/app/products/page.tsx`** — сторінка каталогу з noindex (фільтри)

### 3. Створено SEO-тексти
- **`src/components/seo/SEOTextSection.tsx`** — компонент для відображення SEO-текстів
  - Розміщується ПІД списком товарів (згідно ТЗ)
  - Accordion для згортання/розгортання
  - Популярні ключові запити
  - Приклади контенту для категорій: кросівки, одяг, Nike

### 4. Налаштовано robots.txt та noindex
- **`src/app/robots.ts`** — robots.txt
  - Заборонено індексацію: `/search`, `/api/`, `/admin/`, `/auth/`, `/profile/`, `/test/`
  - Дозволено індексацію SEO-сторінок

### 5. Створено sitemap.xml
- **`src/app/sitemap.ts`** — динамічний sitemap
  - Містить: головна, категорії, бренди, популярні продукти
  - НЕ містить: search, фільтри, технічні сторінки
  - Автоматично оновлюється з API

### 6. Створено приклади SEO-оптимізованих сторінок
- **`src/app/categories/[slug]/page.tsx`** — сторінка категорії
  - 1 × H1, логічні H2-H3
  - Хлібні крихти
  - SEO-текст під товарами
  - JSON-LD structured data
  - Унікальні title/description

- **`src/app/brands/[slug]/page.tsx`** — сторінка бренду
  - Аналогічна структура до категорій
  - Правильна H-ієрархія
  - SEO-контент
  - Structured data

## 📋 Принципи реалізації

### Title (50-60 символів)
```typescript
generateSEOTitle('Nike кросівки', 'ціни, порівняння магазинів')
// Результат: "Nike кросівки — ціни, порівняння магазинів | Wearsearch"
```

### Description (140-160 символів)
```typescript
generateSEODescription(
  'Порівняй ціни на кросівки Nike',
  'в різних магазинах',
  'Знайди найвигіднішу пропозицію онлайн за секунди'
)
```

### H-структура
```html
<h1>Nike кросівки</h1>
<h2>Порівняння цін на кросівки Nike</h2>
<h2>Популярні моделі Nike</h2>
  <h3>Nike Air Force 1</h3>
  <h3>Nike Air Max</h3>
<h2>Як обрати кросівки Nike</h2>
```

## 🚀 Як використовувати

### Для нової категорії
```typescript
import { generateCategoryMetadata } from '@/lib/seo/metadata-utils';

export async function generateMetadata({ params }) {
  return generateCategoryMetadata('Кросівки', {
    description: 'Знайди найкращі кросівки за вигідною ціною',
    keywords: ['кросівки', 'взуття', 'спорт'],
  });
}
```

### Для нового бренду
```typescript
import { generateBrandMetadata } from '@/lib/seo/metadata-utils';

export async function generateMetadata({ params }) {
  return generateBrandMetadata('Nike', {
    description: 'Вся продукція Nike в одному місці',
    keywords: ['Nike', 'Nike кросівки', 'Nike Україна'],
  });
}
```

### Додавання SEO-тексту
```tsx
import { SEOTextSection } from '@/components/seo/SEOTextSection';

<SEOTextSection
  title="Як обрати кросівки"
  content="<p>Ваш SEO-текст тут...</p>"
  keywords={['кросівки', 'як обрати', 'поради']}
/>
```

### Додавання structured data
```tsx
import { JsonLd, generateProductSchema } from '@/lib/seo/structured-data';

<JsonLd data={generateProductSchema({
  id: product.id,
  name: product.name,
  brand: product.brand,
  price: product.price,
  image: product.image_url,
})} />
```

## ⚠️ Що потрібно додатково

### 1. Створити API endpoints (для бекенду)
Необхідні endpoints для sitemap та статичної генерації:
- `GET /api/categories` — список всіх категорій
- `GET /api/categories/popular` — популярні категорії
- `GET /api/categories/{slug}` — дані категорії
- `GET /api/brands` — список всіх брендів
- `GET /api/brands/popular` — популярні бренди
- `GET /api/brands/{slug}` — дані бренду
- `GET /api/products/popular?limit=100` — популярні продукти для sitemap

### 2. Додати поля в API responses
Для кращого SEO необхідні поля:
```typescript
{
  slug: string;           // для URL
  name: string;           // для title
  description: string;    // для meta description
  keywords: string[];     // ключові слова
  image_url: string;      // для OpenGraph
  updated_at: string;     // для sitemap lastModified
}
```

### 3. Наповнити SEO-контент
Додати більше прикладів у `CATEGORY_SEO_CONTENT`:
- Різні категорії товарів
- Популярні бренди
- Популярні моделі
- 300-500 слів якісного контенту

### 4. Додати OpenGraph зображення
Створити:
- `/public/og-image.jpg` (1200×630px) — для головної
- `/public/og/{category}.jpg` — для категорій
- `/public/og/{brand}.jpg` — для брендів

### 5. Налаштувати environment variables
```env
NEXT_PUBLIC_SITE_URL=https://wearsearch.com
NEXT_PUBLIC_API_URL=https://api.wearsearch.com
```

## ✅ Чеклист після впровадження

- [ ] Перевірити View Source — текст є в HTML (SSR працює)
- [ ] Lighthouse SEO ≥ 90
- [ ] Кожна сторінка має один H1
- [ ] Унікальні title/description на кожній сторінці
- [ ] SEO-тексти видимі в HTML (не через JS)
- [ ] robots.txt доступний на `/robots.txt`
- [ ] sitemap.xml доступний на `/sitemap.xml`
- [ ] Всі зображення мають alt
- [ ] Внутрішня перелінковка працює
- [ ] noindex на пошукових сторінках та фільтрах

## 📚 Документація

Всі функції задокументовані JSDoc коментарями. Використовуйте автодоповнення у VSCode для перегляду параметрів та прикладів.

---

**Створено:** 13 січня 2026  
**Статус:** ✅ Базова імплементація готова  
**Наступні кроки:** Налаштувати API endpoints та наповнити SEO-контент
