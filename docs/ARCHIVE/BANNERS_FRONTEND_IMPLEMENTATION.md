# Банери - Документація для фронтенду

## Огляд
Система банерів дозволяє відображати промо-контент на головній сторінці та управляти ним через адміністративну панель.

## 🎯 Основні можливості

### Для користувачів
- ✅ Адаптивна карусель банерів на головній сторінці
- ✅ Автоматична прокрутка (5 секунд між слайдами)
- ✅ Ручне керування стрілками та точками навігації
- ✅ Відстеження показів та кліків
- ✅ Плавні анімації та переходи

### Для адміністраторів
- ✅ Створення/редагування/видалення банерів
- ✅ Завантаження зображень
- ✅ Налаштування заголовків, опису та кнопок
- ✅ Планування показу (дата початку/завершення)
- ✅ Пріоритизація банерів
- ✅ Аналітика (покази, кліки, CTR)
- ✅ Таргетування (всі сторінки, категорія, бренд, продукт)

## 📁 Структура файлів

```
src/
├── types/
│   └── banner.ts                    # TypeScript типи для банерів
├── services/
│   └── bannerService.ts            # API сервіс для роботи з банерами
├── components/
│   ├── BannerCarousel.tsx          # Карусель банерів для головної сторінки
│   └── admin/
│       └── BannerManager.tsx       # Адміністративний компонент управління
├── app/
│   ├── page.tsx                    # Головна сторінка (інтеграція)
│   ├── api/
│   │   └── getHomepageData.ts     # Отримання даних для головної (включаючи банери)
│   └── admin/
│       └── banners/
│           └── page.tsx            # Адмінська сторінка банерів
└── index.css                       # CSS анімації
```

## 🚀 Використання

### 1. Відображення банерів на головній сторінці

Банери автоматично завантажуються разом з іншими даними головної сторінки через BFF endpoint `/api/pages/home`.

```tsx
// src/app/page.tsx
import { BannerCarousel } from '@/components/BannerCarousel';

export default async function HomePage() {
  const homepageData = await getHomepageData();
  
  return (
    <HomeContent
      banners={homepageData.banners}
      // ... інші пропси
    />
  );
}
```

### 2. Компонент карусель

```tsx
// src/components/home/HomeContentClient.tsx
import { BannerCarousel } from '@/components/BannerCarousel';

{banners.length > 0 && (
  <section className="py-8 sm:py-12 md:py-16 bg-black">
    <div className="container mx-auto px-4 sm:px-6">
      <BannerCarousel 
        banners={banners} 
        autoPlayInterval={5000} // опціонально, за замовчуванням 5000мс
      />
    </div>
  </section>
)}
```

### 3. Управління через адмін панель

Перейдіть до адміністративної панелі:
- URL: `/admin`
- Таб: "Банери" (з іконкою 🖼️)

## 📊 API Endpoints

### Публічні (не потрібна авторизація)
- `GET /api/banners` - Отримати активні банери
- `GET /api/pages/home` - Отримати дані головної (включаючи банери)

### Адміністративні (потрібен токен)
- `POST /api/banners` - Створити банер
- `PUT /api/banners/:id` - Оновити банер
- `DELETE /api/banners/:id` - Видалити банер
- `GET /api/banners/:id/analytics` - Отримати аналітику

### Аналітика (автоматично)
- `POST /api/banners/:id/impression` - Відстежити показ
- `POST /api/banners/:id/click` - Відстежити клік

## 🎨 Адаптивний дизайн

### Мобільні пристрої (< 768px)
- Висота банера: 400px
- Текст: 3xl - 4xl
- Компактні кнопки навігації
- Точки навігації меншого розміру

### Планшети (768px - 1024px)
- Висота банера: 500px
- Текст: 4xl - 5xl
- Середні кнопки навігації

### Десктоп (> 1024px)
- Висота банера: 600px
- Текст: 5xl - 6xl
- Повнорозмірні елементи

## 🔧 TypeScript типи

```typescript
interface Banner {
  id: string;
  title: string;
  subtitle?: string | null;
  description?: string | null;
  image_url: string;
  link_url?: string | null;
  link_text: string;
  position: number;
  is_active: boolean;
  start_date?: string | null;
  end_date?: string | null;
  click_count: number;
  impression_count: number;
  target_type: 'all' | 'category' | 'brand' | 'product';
  target_id?: string | null;
  priority: number;
  created_at?: string;
  updated_at?: string;
}
```

## 📈 Аналітика

Відстеження аналітики відбувається автоматично:

1. **Показ (Impression)** - коли банер з'являється на екрані
2. **Клік (Click)** - коли користувач натискає на банер/кнопку

```typescript
// Автоматично викликається в BannerCarousel
useEffect(() => {
  const currentBanner = banners[currentIndex];
  if (currentBanner && !impressionTracked.has(currentBanner.id)) {
    bannerService.trackImpression(currentBanner.id);
    setImpressionTracked(prev => new Set(prev).add(currentBanner.id));
  }
}, [currentIndex, banners]);
```

## ⚙️ Налаштування

### Інтервал автопрокрутки

```tsx
<BannerCarousel 
  banners={banners} 
  autoPlayInterval={7000} // 7 секунд
/>
```

### Вимкнення автопрокрутки

Автопрокрутка автоматично вимикається, коли користувач:
- Натискає на стрілки навігації
- Натискає на точки навігації

## 🎯 Таргетування

Банери можна налаштувати для відображення на певних сторінках:

- **all** - усі сторінки (головна)
- **category** - конкретна категорія (потрібен target_id)
- **brand** - конкретний бренд (потрібен target_id)
- **product** - конкретний продукт (потрібен target_id)

## 📅 Планування

Банери можна планувати:
- `start_date` - не показувати до цієї дати
- `end_date` - не показувати після цієї дати
- `is_active` - вмикає/вимикає банер

## 🔐 Безпека

- Адміністративні операції захищені авторизацією
- Публічні endpoint'и доступні всім
- Аналітика не блокує інтерфейс користувача

## 🐛 Відлагодження

### Перевірка завантаження банерів

```javascript
// У консолі браузера
console.log('Banners loaded:', homepageData.banners);
```

### Перевірка аналітики

```javascript
// У консолі браузера Network tab
// Шукайте запити:
POST /api/banners/{id}/impression
POST /api/banners/{id}/click
```

## 📝 Приклад створення банера

```typescript
const newBanner: CreateBannerRequest = {
  title: "Літня розпродаж 2026",
  subtitle: "Знижки до 70%",
  description: "На весь літній асортимент",
  image_url: "https://example.com/summer-sale.jpg",
  link_url: "/categories/summer",
  link_text: "Купити зараз",
  priority: 10,
  is_active: true,
  target_type: "all",
  start_date: "2026-06-01T00:00:00Z",
  end_date: "2026-08-31T23:59:59Z"
};

await bannerService.createBanner(newBanner);
```

## 🎨 Кастомізація стилів

Компонент використовує Tailwind CSS класи. Основні елементи:

```tsx
// Контейнер банера
className="relative w-full overflow-hidden bg-gradient-to-r from-blue-50 to-purple-50 dark:from-gray-900 dark:to-gray-800 rounded-2xl shadow-lg"

// Текст
className="text-3xl md:text-4xl lg:text-5xl xl:text-6xl font-bold"

// Кнопка CTA
className="inline-flex items-center gap-2 px-6 py-3 md:px-8 md:py-4 bg-white text-gray-900 font-semibold rounded-full hover:bg-gray-100 transition-all transform hover:scale-105 shadow-xl"
```

## 🚦 Стан завантаження

```tsx
{loading && <div>Завантаження банерів...</div>}
{error && <div>Помилка: {error}</div>}
{banners.length === 0 && <div>Банерів немає</div>}
```

## 🔄 Оновлення даних

Банери кешуються на рівні Next.js ISR:

```typescript
// src/app/page.tsx
export const revalidate = 3600; // 1 година
```

Для примусового оновлення:
1. Перейдіть до адмін панелі
2. Оновіть банер
3. Зачекайте 1 годину або перезапустіть dev сервер

## 📞 Підтримка

При виникненні проблем:
1. Перевірте консоль браузера на помилки
2. Перевірте Network tab для API запитів
3. Переконайтеся, що backend API запущено
4. Перевірте `.env` файл для `NEXT_PUBLIC_API_URL`

---

**Дата створення:** 15 січня 2026  
**Версія:** 1.0.0  
**Автор:** WearSearch Team
