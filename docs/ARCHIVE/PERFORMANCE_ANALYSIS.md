# Аналіз продуктивності сайту

## Поточний стан

### Час завантаження (з останнього білду):
- ✅ **Dev сервер:** 1.5 секунди - НОРМАЛЬНО
- ✅ **Production білд:** ~7 секунд - НОРМАЛЬНО для Next.js
- ⚠️ **Компіляція сторінок:** 0.4-4.5 секунди при першому відвідуванні

### Розміри бандлів:
- First Load JS (базовий): **87.3 KB** ✅
- Головна сторінка: **193 KB** ✅
- Сторінка продуктів: **242 KB** ⚠️ (найбільша)
- Адмін панель: **231 KB** ⚠️

### Статистика коду:
- TypeScript/TSX файлів: **253**
- TSX компонентів: **157**
- Загальний розмір: **1.32 MB**

---

## 🔴 Критичні проблеми

### 1. Масові імпорти з lucide-react без оптимізації
**Проблема:** Знайдено **30+ файлів** з прямими імпортами іконок:
```tsx
// ❌ ПОГАНО - імпортує весь модуль
import { Search, X, History, TrendingUp } from 'lucide-react';
```

**Файли з найбільшою кількістю іконок:**
- `EnhancedSearch.tsx` - 6 іконок
- `ImageLightbox.tsx` - 6 іконок  
- `CollectionManager.tsx` - 7 іконок
- `ProductReviews.tsx` - 6 іконок
- `ShareButton.tsx` - 6 іконок

**Вплив:** Кожен імпорт додає ~10-20 KB до бандла, навіть якщо використовується тільки 1 іконка.

**Рішення:** Використовувати динамічні імпорти або tree-shakeable пакет.

---

### 2. Відсутність lazy loading для великих компонентів

**Проблема:** Всі компоненти завантажуються одразу:
- `AdminContent.tsx` (22 KB)
- `ProductsPageContentNew.tsx`
- `EnhancedSearch.tsx`
- `ProductReviews.tsx`

**Рішення:**
```tsx
// ✅ ДОБРЕ - lazy loading
const AdminContent = lazy(() => import('@/components/AdminContent'));
const ProductReviews = lazy(() => import('@/components/ProductReviews'));
```

---

### 3. Великі Radix UI компоненти завантажуються завжди

**Next.config.mjs має оптимізацію**, але не всі пакети включені:
```javascript
optimizePackageImports: [
  'lucide-react', // ✅ Є
  '@radix-ui/react-dialog', // ✅ Є
  '@radix-ui/react-tabs', // ✅ Є
  // ❌ Відсутні:
  // '@radix-ui/react-accordion'
  // '@radix-ui/react-alert-dialog'
  // '@radix-ui/react-collapsible'
]
```

---

### 4. Відсутність code splitting для роутів

**Проблема:** Всі 16 роутів генеруються статично, але великі сторінки не розділені:
- `/products` - 242 KB (найбільша)
- `/admin` - 231 KB
- `/favorites` - 199 KB

**Бандл `/products` включає:**
- FilterPanel (всі фільтри)
- SortDropdown
- PaginationControls
- ProductCard × 20+
- EnhancedSearch

---

### 5. i18n translations завантажуються всі одразу

**Файли перекладів не розділені** за namespace або роутами.

**Рішення:** Розділити на модулі:
```
translations/
  common.json (navigation, buttons)
  products.json (тільки для /products)
  admin.json (тільки для /admin)
  auth.json (тільки для /auth)
```

---

## 🟡 Середньо-пріоритетні проблеми

### 6. TypeScript/ESLint пропускаються при білді
```javascript
typescript: {
  ignoreBuildErrors: true, // ❌ Маскує помилки
},
eslint: {
  ignoreDuringBuilds: true, // ❌ Пропускає перевірки
}
```

**Вплив:** Потенційні баги в продакшені.

---

### 7. Не використовується font optimization

Next.js має вбудовану оптимізацію шрифтів, але не налаштована:
```tsx
// ❌ ЗАРАЗ - завантаження з Google Fonts (повільно)
<link rel="preconnect" href="https://fonts.googleapis.com" />

// ✅ КРАЩЕ - вбудовані шрифти Next.js
import { Inter } from 'next/font/google';
const inter = Inter({ subsets: ['latin', 'cyrillic'] });
```

---

### 8. API calls без timeout та retry

Запити до API можуть зависати:
```tsx
// ❌ Без timeout
await fetch('/api/v1/products');

// ✅ З timeout
await fetchWithTimeout('/api/v1/products', { timeout: 5000 });
```

---

## 🟢 Низько-пріоритетні оптимізації

### 9. Зображення без priority прапорця
Hero images на головній мають завантажуватись першими:
```tsx
<Image src="..." alt="..." priority />
```

### 10. CSS animations на кожному елементі
Багато анімацій через Tailwind додають навантаження на GPU.

### 11. Console.log в продакшені
Хоч `removeConsole: true` налаштовано, є багато `console.error`.

---

## ✅ Що вже добре працює

1. ✅ **SWC minification** увімкнено
2. ✅ **Image optimization** налаштовано (WebP, AVIF)
3. ✅ **React Strict Mode** активний
4. ✅ **Compression** увімкнено
5. ✅ **ISR (revalidate: 3600)** на головній
6. ✅ **Preconnect hints** для зовнішніх доменів
7. ✅ **optimizeCss** експериментальна опція активна
8. ✅ **Code splitting** базовий (87 KB shared chunks)

---

## 📊 Порівняння з еталонами

| Метрика | Ваш сайт | Еталон | Статус |
|---------|----------|--------|--------|
| First Load JS | 87 KB | < 100 KB | ✅ Відмінно |
| Головна | 193 KB | < 200 KB | ✅ Добре |
| Найбільша сторінка | 242 KB | < 250 KB | ⚠️ Прийнятно |
| Dev start | 1.5s | < 3s | ✅ Відмінно |
| Build time | 7s | < 15s | ✅ Відмінно |
| Кількість роутів | 16 | - | ✅ Нормально |

---

## 🚀 План оптимізації (пріоритет)

### Фаза 1: Швидкі покращення (1-2 години)
1. ✅ Додати lazy loading для адмін панелі
2. ✅ Додати lazy loading для ProductReviews
3. ✅ Оптимізувати lucide-react імпорти (топ-10 файлів)
4. ✅ Додати missing Radix UI пакети в optimizePackageImports
5. ✅ Налаштувати next/font замість Google Fonts

### Фаза 2: Структурні зміни (3-4 години)
6. ⏳ Розділити i18n translations на модулі
7. ⏳ Додати dynamic imports для FilterPanel
8. ⏳ Додати timeout wrapper для fetch()
9. ⏳ Увімкнути TypeScript checking (виправити помилки)

### Фаза 3: Поліпшення UX (2-3 години)
10. ⏳ Додати skeleton loaders для всіх async компонентів
11. ⏳ Оптимізувати анімації (will-change, transform3d)
12. ⏳ Додати priority до hero images
13. ⏳ Налаштувати Service Worker для offline

---

## 💡 Очікувані результати

**Після Фази 1:**
- 📉 Розмір бандла: **-30-50 KB** (-15-20%)
- ⚡ Швидкість завантаження: **+20-30%**
- 🎯 Time to Interactive: **-0.5-1s**

**Після всіх фаз:**
- 📉 Загальний розмір: **-70-100 KB** (-30%)
- ⚡ LCP (Largest Contentful Paint): **< 2.5s**
- 🎯 FID (First Input Delay): **< 100ms**
- 📊 Performance score: **90+/100**

---

## 🔍 Чому сайт здається повільним?

### Можливі причини, які бачить користувач:

1. **При першому відвідуванні:**
   - Next.js компілює сторінку (0.4-4.5s)
   - Завантажуються всі Radix UI компоненти
   - Завантажується весь lucide-react набір іконок
   - Завантажуються всі translations

2. **При переході між сторінками:**
   - Client-side navigation швидкий
   - Але нові компоненти завантажуються без lazy loading
   - Немає skeleton loaders (відчуття блокування)

3. **На повільному інтернеті:**
   - 242 KB для /products може завантажуватись 2-5 секунд
   - Немає progressive loading
   - Всі зображення завантажуються одразу (хоч є lazy атрибути)

---

## ✍️ Рекомендації

**Якщо час обмежений - почніть з Фази 1** (5 пунктів вище).  
Це дасть **максимальний ефект при мінімальних затратах часу**.

**Для production готовності** потрібні всі 3 фази + тестування на реальних пристроях.

**Найкритичніше:** lazy loading для Admin та оптимізація lucide-react.
